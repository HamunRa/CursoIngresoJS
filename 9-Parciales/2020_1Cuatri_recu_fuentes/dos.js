function mostrar()
{
  /*
 // variables
 var nombreIngresado; //nombre
 var edadIngresada;   //edad
 var sexoIngresado;   //sexo
 var cantidad ;       //cantidad
 var promedio ;       //promedio (seguramente hacer mas)
 var descuento ;
 var contador = 0;    //contador (seguramente hacer mas)
 var mayorContador = 0;
 var bandera = 0;     //bandera
 var respuesta = "si";  //para ingresar datos cuantas veces quiera

 // para que el usuario ingrese cuantas veces quiera (cambiar por contador si es limitado)
 while(respuesta == "si") // contador < (numero)
      {	


        respuesta = prompt("Quiere ingresar otra carga? (si o no)");
       respuesta = respuesta.toLowerCase();

       while(!(respuesta == "si" || respuesta == "no"))
       {
         respuesta = prompt("Quiere ingresar otra carga? (si o no)");
         respuesta = respuesta.toLowerCase();
       }

       //contador++
     }

     acumulador = importe + acumulador; // para que se sumen los datos
      

      // pedir dato de nombre

      nombreIngresado = prompt("Ingrese su nombre");

     nombreIngresado = nombreIngresado.toLowerCase;

     while(!(isNaN(nombreIngresado)))
     {
        nombreIngresado = prompt("Error. Ingrese su nombre");
        nombreIngresado = nombreIngresado.toLowerCase;
     }




     // Pedir dato de edad

     edadIngresada = parseInt(prompt("Ingrese su edad. (Mayor de (numero) años)"));

      while (edadIngresada < o > (numero))
     {
       edadIngresada = parseInt(prompt("Error. Ingrese su edad. (Mayor de 12 años)"));
     }



     // pedir dato de sexo

     sexoIngresado = prompt("Ingrese su sexo. (f o m)");

     sexoIngresado = sexoIngresado.toLowerCase();

     while (! (sexoIngresado == "f" || sexoIngresado == "m"))
     {
       sexoIngresado = prompt("Error. Ingrese su sexo. (f o m)");

       sexoIngresado = sexoIngresado.toLowerCase();
     }



     // validar un dato especifico

     (var especifica) = prompt("Ingrese la (var especifica). (  ,  o )");

     (var especifica) = (var especifica).toLowerCase();

     while(!(var  == "Texto especifico" || var == "Texto especifico" || var == "Texto especifico"))
     {
       (var especifica) = prompt("Error. La (var especifica) ingresada no existe. Ingrese la (var especifica) (  ,  o )");
       (var especifica) = (var especifica).toLowerCase();
     }

     //sumar contadores especificos

     switch(var)
     {
       case "1":
       contador1++;
       break;

       case "2":
       contador2++;
       break;

       case "3":
       contador3++;
       break;
     }



     //validar un rango de cantidades especifica

     (cantidad) = parseInt(prompt("Ingrese la cantidad (entre (numero) y (numero)"));

     while(cantidad < (numero) || cantidad > (numero))
     {
       (cantidad) = parseInt(prompt("Error. Ingrese la cantidad (entre (numero) y 1500 (numero)"));
     }
     


     //como usar bandera 

   if( (var1) > o < (dato pedido) || bandera=0)
     {
       (minimo) = (dato pedido);
       (maximo) = (var1);

     }

     if((var) < (var) && (var) == ("Texto especifico") || bandera = 0)
     {
       (var) = (var);
       bandera = 1;
     }


     // AFUERA DEL WHILE 

     // que contador es mayor
   if(contador1 > contador2 && contador1 > contador3)
   {
     mayorContador = "contador 1";
   }
   else
   {
     if(contador2 > contador3)
     {
       mayorContador = "contador 2";
     }
     else
     {
       mayorContador = "contador3";
     }
   }

 document.write(" El mayor () es: " + mayorContador + "<br>");

 // PROMEDIO

 promedio = cantidad / contador ;

 if(isNaN(promedio))

 {
   promedio = 0;
 }

 document.write("Promedio de : " + promedio + "<br>");




 // descuento de importe

 if(cantidad  > (numero))
 {
   descuento = (numero); // o aumento usar numeros decimales
 }
 else if(cantidad >= (numero) && cantidad <= (numero) )
 {
   descuento = (numero); // o aumento usar numeros decimales
 }
 else
 {
   descuento = (numero); // o  aumento usar numeros decimales
 }

 precioTotal = importe - (importe * descuento/100);
 precioTotal = importe + (importe * descuento/100);


 document.write("El descuento o aumento fue de: " + descuento_aumento + " y el importe final fue de:  " + precioTotal);
*/
}

